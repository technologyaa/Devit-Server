# ==================================
# Spring Boot Configuration
# ==================================

# Server Port
server.port=8080

# ==================================
# Database Configuration (MySQL)
# ==================================

# JDBC URL - replace YOUR_DB_NAME with your actual database name
spring.datasource.url=jdbc:mysql://localhost:3306/websocket_chat_db?serverTimezone=UTC&characterEncoding=UTF-8
spring.datasource.username=root
spring.datasource.password=hen061813*
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ==================================
# JPA/Hibernate Configuration
# ==================================

# MySQL Dialect
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# DDL Auto: update allows Hibernate to update the schema (tables) based on entities
# Use 'none' or 'validate' in production environment
spring.jpa.hibernate.ddl-auto=update

# Show SQL queries in console (useful for debugging)
spring.jpa.show-sql=true

# Format SQL output
spring.jpa.properties.hibernate.format_sql=true

# ==================================
# Redis Configuration
# ==================================
# Redis를 사용하려면 주석을 해제하고 Redis 서버를 실행한 후 RedisConfig.java의 어노테이션 주석도 해제하세요.
# spring.data.redis.host=localhost
# spring.data.redis.port=6379

# ==================================
# Mail Configuration
# ==================================
# 이메일 기능을 사용하려면 주석을 해제하고 환경변수(MAIL_USERNAME, MAIL_PASSWORD)를 설정한 후 EmailConfig.java의 어노테이션 주석도 해제하세요.
# spring.mail.host=smtp.gmail.com
# spring.mail.username=${MAIL_USERNAME}
# spring.mail.password=${MAIL_PASSWORD}
# spring.mail.properties.mail.smtp.auth=true
# spring.mail.properties.mail.smtp.starttls.enable=true

# ==================================
# OAuth2 Configuration
# ==================================
spring.security.oauth2.client.registration.google.client-id=877657680277-mhmphpq00ldjm3jnqm8mc3ncocj4ioth.apps.googleusercontent.com
spring.security.oauth2.client.registration.google.client-secret=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
spring.security.oauth2.client.registration.google.scope[0]=profile
spring.security.oauth2.client.registration.google.scope[1]=email
# redirect-uri는 Spring Security가 자동으로 생성하므로 명시하지 않음
# Google Cloud Console에 다음 URI를 등록해야 합니다:
# http://localhost:8080/login/oauth2/code/google

# ==================================
# JWT Configuration
# ==================================
# 운영 환경에서는 반드시 환경변수(JWT_SECRET)를 사용하세요.
# 예: export JWT_SECRET=your-strong-secret-key
# 개발 환경에서만 사용하는 임시 시크릿 키입니다.
Jwt.secret=mySecretKeyForDevelopmentOnlyPleaseChangeInProduction
