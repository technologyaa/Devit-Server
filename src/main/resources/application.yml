spring:
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: 877657680277-mhmphpq00ldjm3jnqm8mc3ncocj4ioth.apps.googleusercontent.com   # 꼭 실제 client-id로 바꿔주세요
            client-secret: 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   # 실제 client-secret 입력
            scope:
              - profile
              - email
        provider:
          google:
            user-info-uri: https://www.googleapis.com/oauth2/v3/userinfo

  datasource:
    url: jdbc:mysql://localhost:3306/oauthdb?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: ""                # 비밀번호 없으면 빈 문자열로
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update          # 엔티티 기준으로 테이블 자동 생성/업데이트
    show-sql: true               # SQL 쿼리 로그 출력
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
